#!/usr/bin/env bash

pid=$1;

./rcon.py localhost {{ rcon_port }} "{{ rcon_password }}" "say Server will reload in 10 seconds!";
sleep 10
./rcon.py localhost {{ rcon_port }} "{{ rcon_password }}" reload;